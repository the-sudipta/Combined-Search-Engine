/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "random.imagecdn.app",
            "img.icons8.com"
        ]
    }
};

export default nextConfig;
